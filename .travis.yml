dist: trusty
sudo: required # due to docker
language: sh

env:
  global:
    - DOCKER_COMPOSE_VERSION=1.13.0
  matrix:
    - DIST=busybox
    - SH=ash DIST=alpine
    - SH=bash DIST=alpine
    - SH=bash DIST=debian
    - SH=dash DIST=alpine
    - SH=dash DIST=debian
    - SH=ksh DIST=alpine
    - SH=ksh DIST=debian
    - SH=zsh DIST=alpine
    - SH=zsh DIST=debian

before_install:
  # latest docker 
  - sudo apt-get update
  - sudo apt-get -y -o Dpkg::Options::="--force-confnew" install docker-engine
  - docker --version
  # docker compose
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin

script:
  - if [ -z "${SH}" ]; then docker-compose run "${DIST}"; else docker-compose run "${SH}_${DIST}"; fi
